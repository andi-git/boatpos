<?xml version="1.0" encoding="UTF-8" ?>
<entity-mappings xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence/orm http://java.sun.com/xml/ns/persistence/orm_2_1.xsd"
                 version="2.1">
    <named-query name="journal.sumBoatPeriod">
        <query>SELECT SUM(r.pricePaidBefore), SUM(r.pricePaidAfter)
               FROM   RentalEntity r
               WHERE  r.day BETWEEN :start AND :end
                 AND  r.boat = :boat
                 AND  r.finished = true
                 AND  r.deleted = false
        </query>
    </named-query>
</entity-mappings>
